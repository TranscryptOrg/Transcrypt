	(function () {
		var a = 1;
		/*
		<DIV ID="STUDENTCONTAINER">
		    {SELF.PROPS.STUDENTS.MAP((S) => (
		        <STUDENTTILE STUDENT={S}  KEY={S.SEARCHVAL}  />
		    ))}
		</DIV>
		*/
		/*
		<div id="studentcontainer">
		    {self.props.students.map((s) => (
		        <studenttile student={s}  key={s.searchval}  />
		    ))}
		</div>
		*/
		var b = 2;
		__pragma__ ('<all>')
			__all__.a = a;
			__all__.b = b;
		__pragma__ ('</all>')
	}) ();
